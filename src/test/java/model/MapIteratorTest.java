/*

import model.Enumerations.TileColourEnum;
import org.junit.Before;
import org.junit.Test;

public class MapIteratorTest {
/*
    private Tile[][] map;
    private Game currGame;
    private Deck<WeaponCard> deck;

    @Before
    public void setup() {
        for (int i = 0; i < 3; i++)
            for (int j = 0; j < 3; j++)
                map[i][j] = new SpawnTile(j, i, TileColourEnum.RED, deck, currGame);
    }

    @Test
    public void printMap() {
        Tile currTile = map[0][0];
        for (MapIterator itr = new MapIterator(map[0][0]); itr.hasNext(); ) {
            System.out.println("(" + currTile.getX() + "," + currTile.getY() + ")");
            currTile = itr.next();
        }
        System.out.println("(" + currTile.getX() + "," + currTile.getY() + ")");
    }
}
 */
